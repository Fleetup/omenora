# STEP 2.1 ANALYSIS — Add `stripeCompatCalendarBumpPriceId` to runtimeConfig
**Generated:** 2026-05-01 | **Scope:** Read-only audit of `nuxt.config.ts` and related files

---

## 1. File Path and Line Count

- **Path:** `/Volumes/ESSD/Projects/Augur-V1/augur/nuxt.config.ts`
- **Total lines:** 261

---

## 2. The `runtimeConfig` Block — Full Quote with Line Numbers

```
@/Volumes/ESSD/Projects/Augur-V1/augur/nuxt.config.ts:14-44
  runtimeConfig: {
    // Empty string defaults — Nitro reads the actual process.env at container
    // startup (runtime), not at Docker build time when secrets are not present.
    // Railway variable names stay exactly as set: ANTHROPIC_API_KEY, etc.
    anthropicApiKey: '',
    stripeSecretKey: '',
    resendApiKey: '',
    supabaseUrl: '',
    supabaseServiceKey: '',
    stripeDailyPriceId: '',
    stripeCompatPlusPriceId: '',
    stripeCompatSinglePriceId: '',
    emailJobSecret: '',
    cronSecret: '',
    inngestEventKey: '',
    inngestSigningKey: '',
    stripeWebhookSecret: '',
    resendWebhookSecret: '',
    redisUrl: '',
    public: {
      stripePublishableKey: '',
      supabaseUrl: '',
      supabaseAnonKey: '',
      siteUrl: 'https://omenora.com',
      sentryDsn: '',
      tiktokPixelId: '',
      metaPixelId: '',
      posthogKey: '',
      googlePlacesKey: process.env.NUXT_PUBLIC_GOOGLE_PLACES_KEY || '',
    },
  },
```

Block span: **lines 14–44** (opening `runtimeConfig: {` through closing `},`).

---

## 3. Exact Line for `stripeCompatPlusPriceId` with Context

```
@/Volumes/ESSD/Projects/Augur-V1/augur/nuxt.config.ts:21-27
    supabaseUrl: '',
    supabaseServiceKey: '',
    stripeDailyPriceId: '',
    stripeCompatPlusPriceId: '',       ← LINE 24
    stripeCompatSinglePriceId: '',
    emailJobSecret: '',
    cronSecret: '',
```

**Target line: 24.**

---

## 4. Formatting Style of Sibling Keys

| Attribute | Value |
|---|---|
| **Quotes around empty string fallbacks** | Single quotes `''` |
| **Quotes around non-empty string values** | Single quotes (e.g. `'https://omenora.com'`) |
| **Trailing comma after last private key** (`redisUrl`) | Yes — `redisUrl: '',` has a trailing comma |
| **Trailing comma after last public key** (`googlePlacesKey`) | Yes — `googlePlacesKey: process.env... || '',` has a trailing comma |
| **Indentation** | 2 spaces (4 spaces inside the `public` sub-object, relative to the file root) |
| **Key ordering** | Insertion order (not alphabetical, not strictly categorical). Stripe-related keys are loosely grouped but not sorted: `stripeSecretKey` (line 19), `stripeDailyPriceId` (23), `stripeCompatPlusPriceId` (24), `stripeCompatSinglePriceId` (25), `stripeWebhookSecret` (30). |

The natural insertion point for `stripeCompatCalendarBumpPriceId` — by category proximity — is **after line 25** (`stripeCompatSinglePriceId: '',`), keeping all compat-related price IDs together.

---

## 5. TypeScript Type Declarations Constraining `runtimeConfig` Keys

**Yes — auto-generated file:**

```
@/Volumes/ESSD/Projects/Augur-V1/augur/.nuxt/types/runtime-config.d.ts:1-46
```

This file is **generated by `nuxt prepare`** (or any build/dev command). It derives its `SharedRuntimeConfig` interface directly from the keys declared in `nuxt.config.ts`. Relevant excerpt:

```typescript
interface SharedRuntimeConfig {
  // ...
  stripeDailyPriceId: string,        // line 23
  stripeCompatPlusPriceId: string,   // line 25
  stripeCompatSinglePriceId: string, // line 27
  // ...
}
```

**No hand-authored `.d.ts` files** constrain `runtimeConfig` outside `.nuxt/`. The only hand-authored type file found is `augur/types/pixels.d.ts` which is unrelated to runtimeConfig.

**Implication:** Adding `stripeCompatCalendarBumpPriceId: ''` to `nuxt.config.ts` and then running `npx nuxt prepare` will automatically add `stripeCompatCalendarBumpPriceId: string` to `SharedRuntimeConfig`. No manual type editing required.

---

## 6. Existing References to `NUXT_STRIPE_COMPAT_CALENDAR_BUMP_PRICE_ID`

Grep across all source files (excluding `node_modules` and `.nuxt`):

**Results: 3 hits — all in `/Volumes/ESSD/Projects/Augur-V1/augur/STEP_2_ANALYSIS.md` only.**

This is a pre-existing analysis markdown document (not source code). Zero hits in any `.ts`, `.vue`, `.js`, `.mjs`, `.json`, or `.env*` file. The key does **not** yet exist anywhere in the runtime source.

---

## QUESTIONS FOR HUMAN

1. **Insertion position confirmed?** The analysis suggests inserting after `stripeCompatSinglePriceId` (line 25) to keep all compat price IDs together. Is that the intended position, or should it go elsewhere (e.g., at the end of the private keys before `public:`)?

2. **Consumer API route:** Which server API file will read `config.stripeCompatCalendarBumpPriceId`? Is it a new file, or an addition to the existing `create-compatibility-payment.post.ts`? This determines whether a `npx nuxt prepare` is sufficient or whether a new API endpoint also needs scaffolding.

3. **Railway variable:** Has `NUXT_STRIPE_COMPAT_CALENDAR_BUMP_PRICE_ID` already been added to the Railway dashboard, or does that still need to happen after this change?
